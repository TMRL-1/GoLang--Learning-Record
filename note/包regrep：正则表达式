导入（“regexp”）
编译，必须编译
FindString ， FindAllString
火柴绳
ReplaceAllString
分割


编译和必须编译
编译正则表达式
Compile 返回一个 *Regexp 对象和一个错误，而 MustCompile 在编译失败时会直接 panic

火柴绳
检查字符串是否匹配正则表达式。

和
查找匹配的字符串
找到返回第一个匹配项，找到你的

ReplaceAllString
用于替换匹配的字符串

分割
根据正则表达式分割字符串

基本语法:
.：匹配任意单个字符（除了换行符）。
*：匹配前面的字符 0 次或多次。无或有
+：匹配前面的字符 1 次或多次。有
?：匹配前面的字符 0 次或 1 次。无或有一个
\d：匹配数字字符（等价于 [0-9]）。
\w：匹配字母、数字或下划线（等价于 [a-zA-Z0-9_]）。
\s：匹配空白字符（包括空格、制表符、换行符等）。
[]：匹配括号内的任意一个字符（例如 [abc] 匹配 a、b 或 c）。
^：匹配字符串的开头。
$：匹配字符串的结尾。
\ (反斜杠)转义字符，使后面的字符失去特殊含义
{n}（花括号）
精确匹配n次示例：a{3} 匹配 "aaa"
{n,}至少匹配n次
示例：a{2，}“aa”，“aaa”
{n，m}匹配 n m
示例：a{2,4}“aa”，“aaa”，“aaaa”
\D非数字 \W非单词字符 \S非空白字符

() (圆括号)
定义子表达式或捕获组
示例：（ab）+yodo“ab”，“abab”

| (竖线)
表示"或"关系
示例：猫|狗我知道“猫”“狗”

\b
匹配单词边界
示例：\bcat\b“猫”你的“类别”
\B
匹配非单词边界
示例：\Bcat\B“散”“猫”“猫”“猫”

\. 匹配实际的点号而不是任意字符
^abc 匹配以 "abc" 开头的字符串
xyz$ 匹配以 "xyz" 结尾的字符串

\n匹配换行符
\t匹配制表符
\r匹配回车符
\f匹配换页符
\v匹配垂直制表符

贪婪与非贪婪量词
默认情况下，量词(*, +, ?, {})是贪婪的，会尽可能多地匹配字符。在量词后加?可使其变为非贪婪(懒惰)模式：
*?：零次或多次，但尽可能少
+?：一次或多次，但尽可能少
??：零次或一次，但尽可能少
{n,m}?：n到m次，但尽可能少

正向和负向预查
(?=...) (正向肯定预查)
匹配后面跟着特定模式的位置
示例：Windows（？=95|98）你看你的“Windows”
(?!...) (正向否定预查)
匹配后面不跟着特定模式的位置
示例：Windows（？！95|98）我的95个“Windows”
(?<=...) (反向肯定预查)
匹配前面是特定模式的位置
示例：（？<=95|98）Windows you you 95 98“Windows”
(?<!...) (反向否定预查)
匹配前面不是特定模式的位置
示例：（？<！95|98）Windows*******************************************************************************************************************************************************************************************
